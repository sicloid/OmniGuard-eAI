name: Unprivileged foundation checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  contracts:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14.7'
      - run: python -m pip install --require-hashes -r requirements-ml.lock
      - run: python -m pip install -e . --no-deps --no-build-isolation
      - run: python -c "import sklearn, numpy, scipy, joblib; print(sklearn.__version__, numpy.__version__, scipy.__version__, joblib.__version__)"
      - run: ruff check .
      - run: ruff format --diff lab/leakage_probe.py tests/test_leakage.py
      - run: ruff format --check .
      - run: python -m unittest discover -s tests -v
      - run: python -m stubs
      - name: Check lab shell syntax (no privileged execution)
        if: runner.os == 'Linux'
        run: |
          for script in lab/*.sh; do bash -n "$script"; done
          shellcheck -x lab/*.sh
  platform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14.7'
      - run: python platform/init_secrets.py
      - run: docker compose -f platform/compose.yaml up -d --wait --wait-timeout 180
      - run: python platform/smoke.py
      - name: Clean up this job's services and volumes
        if: always()
        run: docker compose -f platform/compose.yaml down -v
