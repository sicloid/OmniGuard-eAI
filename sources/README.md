# R2 — Şükrü

Implement PCAP and live adapters yielding `core.schema.PacketTuple`.
Validate configured LAN direction, internal device mapping, L3 length and fragment
semantics against deterministic oracle captures. Payload bytes do not enter contracts.
